dm-snap-fiemap
==============

add fiemap ioctl to dm-snap to export extents in the snapshort target

Currently only for kernel 2.6.32

Will add support for dm-thin target in the furture.


Usage
===============

As the same as that for ordinary files.

See https://www.kernel.org/doc/Documentation/filesystems/fiemap.txt for reference.
